{
  "name": "wherearethegrants-com",
  "private": true,
  "scripts": {
    "test": "node --test conformance.test.mjs",
    "prepare:assets": "mkdir -p .deploy-assets && cp index.html styles.css robots.txt sitemap.xml .deploy-assets/",
    "deploy": "npm run test && npm run prepare:assets && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.127.1"
  }
}
